# Rust build artifacts
/target
**/*.rs.bk
Cargo.lock.orig

# Benchmark outputs (regenerated by runs; not source)
/result
/raw
/evaluation
/weights
/plots

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
~$*

# Smart-preset sweep scratch (detached-run logs); keep scripts + _smart/results/
_smart/*.log
_smart/*.err

# Python (kept model defs are tracked explicitly; ignore caches/venv)
__pycache__/
*.pyc
.venv/

# Phase-2 scratch run dirs (timing/correctness intermediates); keep the harness + iterations.md
/_phase2/*/
# Stray mangled-redirect logs
C*Temp*.log
